"use strict"; // treat all JS code as newer version

// alert( 3 + 3); // we are using nodejs, not browser 

console.log(3
    + 
3); // Code readability should be high

console.log('nora');

let name = 'nora'; // string data type 
const age = 29; // number data type 
let siLoggedIn = false; // boolean data type 
let state = null; // null => is standalone value 

// object 
console.log(typeof null);
console.log(typeof name);
